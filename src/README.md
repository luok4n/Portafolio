# Source

- `frontend/portfolio-web` — Angular 22 application (phase 5).
- `services/portfolio-api` — ASP.NET Core solution (phase 3).

Both are created in their respective phases; see [docs/development-plan.md](../docs/development-plan.md).
