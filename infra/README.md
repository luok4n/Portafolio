# Infrastructure

- `docker/` — Dockerfiles and compose overrides (phase 7).
- `kubernetes/` — manifests, created only if phase 11 concludes Kubernetes is justified (phase 12).
- `scripts/` — helper scripts.

See [docs/development-plan.md](../docs/development-plan.md).
